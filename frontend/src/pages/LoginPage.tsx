/**
 * Placeholder for future login. When auth is implemented,
 * replace with form and redirect after success.
 */
export function LoginPage() {
  return (
    <div className="flex min-h-[50vh] flex-col items-center justify-center">
      <h1 className="text-xl font-semibold text-slate-900">Login</h1>
      <p className="mt-2 text-sm text-slate-600">
        Authentication will be added in a future release.
      </p>
    </div>
  );
}
